This upload changed the 3 PARTIAL banks (BOP/AKBL/BIPL) — they now have rich 6-year series. But I'm seeing issues. Let me compare against the canonical file and run the scorer to see real impact:Verifying file equivalence with deployed canonical versionVerifying file equivalence with deployed canonical versionThe uploaded file appears identical to the canonical. Confirming a full byte-level diff:Verified file integrity and flagged unit inconsistenciesVerified file integrity and flagged unit inconsistenciesChecked — and the key result is that this upload is identical to the bank_ig2_inputs.json already in your repo. Normalized JSON comparison: every field of all 12 banks matches the canonical file byte-for-byte in content (only whitespace/key-order differs). So there's nothing new to deploy here.
What that means for the three partial banks, concretely:

BOP / AKBL / BIPL still have eps: None, equity: None, npl: None, cfo: None, branches: None. They have rich 6-year series for markup / net-spread / pbp / pat / assets / loans / deposits / provisions (which is why they score at /34 — BIPL 85.3%, AKBL 76.5%, BOP 47.1%), but the fields the valuation needs aren't there. So they correctly stay on the "Bank valuation pending" note from v5.37 — no valuation panel, which is right.
The 9 full banks are unchanged, so they still reproduce MEBL 45/48 … NBP 26/48 and carry their valuations.

One thing worth flagging for when you do complete these three — a units landmine. The 3 partials are entered in PKR billions (BOP pat₂₄ = 6.8, assets = 2412), while the 9 full banks are in PKR millions (MEBL pat₂₄ = 103,719, assets = 3,912,189). That mismatch is harmless to the current score because every IG2 metric is a within-bank ratio or CAGR (units cancel). But the valuation math is not unit-invariant: it computes shares = pat ÷ eps, bvps = equity ÷ shares, and that only comes out right if pat/equity are in millions and eps in rupees/share (that's how MEBL's ~1.81bn shares and ~140 bvps fall out correctly). If you add eps to BOP/AKBL/BIPL while their pat/equity stay in billions, the valuation will be off by ~1000× — the same class of scale bug as the earlier risk-free mix-up.
So to actually enable valuation for the three, the inputs need: per-year eps (PKR/share) + equity, and the whole partial block converted to PKR millions to match the other nine (multiply the current bn values by 1000). Optionally npl and cfo series too, which would also lift them off /34 toward the full /48. Until then, v5.37 already handles them gracefully — no code change or redeploy needed from this file.
